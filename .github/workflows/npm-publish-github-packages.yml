name: Package Publish Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publish disabled
        run: echo "Package publishing is disabled until an explicit later decision."
